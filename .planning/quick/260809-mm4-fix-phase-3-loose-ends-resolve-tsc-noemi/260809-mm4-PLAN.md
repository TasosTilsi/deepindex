---
phase: 260809-mm4-fix-phase-3-loose-ends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli.ts
  - src/health.ts
  - tests/smoke.test.ts
autonomous: true
requirements:
  - CMD-03
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "`pnpm exec tsc --noEmit` exits 0 (no strict-mode errors)"
    - "`pnpm run smoke` runs build → status → retrieve on fixtures/sample-repo, exits 0, prints health JSON + top-K files"
    - "`pnpm test` stays all green after both fixes"
  artifacts:
    - src/cli.ts (repair stages loop type-safe)
    - src/health.ts (loadConfig match-group guards)
    - tests/smoke.test.ts
  key_links:
    - "smoke test spawns tsx src/cli.ts with absolute bin path (pattern from tests/cli.test.ts) so it runs under any cwd"
    - "health.ts guard returns DEFAULT_HEALTH_CONFIG consistently with the existing no-match path"
---

<objective>
Close phase-3 loose ends: eliminate the two `tsc --noEmit` strict-mode errors (`noUncheckedIndexedAccess`) and ship the missing smoke test that ROADMAP phase-3 criterion #5 requires.

Purpose: `tsc --noEmit` must pass for the v2 merge phases to build on a clean typecheck; `pnpm run smoke` is a declared success criterion that has never been runnable.
Output: type-safe src/cli.ts + src/health.ts, and a runnable tests/smoke.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Root cause is `tsconfig.json` `noUncheckedIndexedAccess: true`:
- `result.stages[i]` → `RepairStageResult | undefined` (src/cli.ts:107)
- `sectionMatch[1]` → `string | undefined` (src/health.ts:61), so `block.match(...)` at :62 errors
- `valueMatch[1]` → `string | undefined` (src/health.ts:67), so `Number.parseInt(raw, 10)` at :68 errors
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix strict-mode tsc errors in src/cli.ts and src/health.ts</name>
  <files>src/cli.ts, src/health.ts</files>
  <action>
    Fix the two `noUncheckedIndexedAccess` violations. Do NOT disable the flag.

    src/cli.ts (~line 106-109, `repair` command non-JSON branch): the `for (let i = 0; i < result.stages.length; i++)` loop indexes `result.stages[i]` into a possibly-undefined `s`. Replace the indexed loop with a for-of over entries so no index access occurs:
    `for (const [i, s] of result.stages.entries()) { console.log(...same template...); }`
    This preserves the `stage ${i + 1}:` label exactly. No other behavior change.

    src/health.ts `loadConfig` (~lines 59-72): both match-group captures are possibly undefined.
    - `const block = sectionMatch[1];` → use `const block = sectionMatch[1] ?? '';` — an empty block matches no `repair_below` value, so the existing `if (!valueMatch) return { ...DEFAULT_HEALTH_CONFIG };` handles it identically to a section with no value.
    - `const raw = valueMatch[1];` → use `const raw = valueMatch[1] ?? '';` — `Number.parseInt('', 10)` is NaN, which the existing `if (!Number.isFinite(n))` throw path already treats as malformed config (consistent with the "eighty" example comment). Keep the existing throw behavior; do not swallow it.
    Do not alter the throw-on-non-numeric contract. Do not touch other functions.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    `pnpm exec tsc --noEmit` exits 0. The repair command still prints `before:`/`after:` labels with per-stage ok/fail lines (tests/cli.test.ts repair test unaffected), and loadConfig still throws TypeError on `repair_below = "eighty"` and returns defaults on missing [health] section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/smoke.test.ts so pnpm run smoke passes</name>
  <files>tests/smoke.test.ts</files>
  <action>
    Create a plain tsx script (NOT a vitest test — `package.json` runs it via `tsx tests/smoke.test.ts`). It must not import from vitest.

    Follow the spawn pattern in tests/cli.test.ts lines 7-30: `FIXTURE = resolve(process.cwd(), 'fixtures/sample-repo')`, a `run(args)` helper that spawns the ABSOLUTE tsx binary (`resolve(process.cwd(), 'node_modules/.bin/tsx')`) with `['src/cli.ts', ...args]` and resolves `{ code, stdout, stderr }` on close. Use a temp DB via `mkdtempSync(join(tmpdir(), 'ctx-smoke-'))` and clean it up with `rmSync(dir, { recursive: true, force: true })` at the end.

    Sequence (mirrors ROADMAP phase-3 criterion #5):
    1. `build FIXTURE --db <dbPath>` → assert exit code 0, else print stderr and exit 1.
    2. `status FIXTURE --db <dbPath>` → assert exit code is 0 or 1 (health score can legitimately be below threshold), parse stdout as JSON, print the health JSON (score, dimensions, issues keys) to stdout, assert it has `score` property.
    3. `retrieve auth --db <dbPath> --top-k 5` → assert exit code 0, print the top-K file list to stdout, assert stdout matches /\.ts/ (as tests/cli.test.ts line 76 does).
    4. On any assertion failure: `console.error(...)` with the failing step + stderr, then `process.exit(1)`. On full success print a trailing `smoke ok` line and exit 0.

    Use `node:assert/strict` or inline `if (!cond) { fail(step, stderr); }` — a `fail` helper that prints and exits 1. The test file sits in `tests/` which tsconfig excludes from `tsc --noEmit`, so it will not affect the typecheck gate, but keep the types clean anyway.
  </action>
  <verify>
    <automated>pnpm run smoke</automated>
  </verify>
  <done>
    `pnpm run smoke` exits 0, prints health JSON (score/dimensions/issues), prints top-K file paths for query "auth", and ends with `smoke ok`.
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0 (Task 1)
- `pnpm run smoke` exits 0 (Task 2)
- `pnpm test` still passes (existing suite, esp. tests/cli.test.ts repair + health tests)
- Full gate: `pnpm exec tsc --noEmit && pnpm run smoke && pnpm test`
</verification>

<success_criteria>
- No strict-mode type errors remain; `pnpm exec tsc --noEmit` clean.
- ROADMAP phase-3 success criterion #5 is real: `pnpm run smoke` runs build → status → retrieve on fixtures/sample-repo, exits 0, prints health JSON + top-K files.
- Existing vitest suite (88 tests) unaffected.
</success_criteria>

<output>
Create `.planning/quick/260809-mm4-fix-phase-3-loose-ends-resolve-tsc-noemi/260809-mm4-SUMMARY.md` when done
</output>

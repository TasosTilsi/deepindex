---
phase: 04-merge-dataflow-multilang
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/graph/db.ts
  - src/parser/languages/index.ts
  - src/parser/symbol-walker.ts
autonomous: true
requirements:
  - MLNG-01
  - MLNG-02
  - MLNG-03
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Files ending in .java, .cpp, .go, .rs are detected and parsed.
    - Symbols (classes, functions) in these languages are normalized to 'class' or 'method'.
    - Complexity heuristics (keyword counts) are calculated for these languages.
  artifacts:
    - src/parser/languages/index.ts (Node-type maps)
  key_links:
    - Language Extension $\to$ Tree-Sitter WASM $\to$ Symbol Walker
---

<objective>
Expand the symbol indexing capabilities to cover the full Cobi scope of 8 languages.

Purpose: Enable DeepIndex to map symbols and imports in non-TS/JS/Py projects.
Output: Multi-language symbol extraction pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-merge-dataflow-multilang/04-CONTEXT.md
@.planning/phases/04-merge-dataflow-multilang/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-lang WASM Grammar Integration</name>
  <files>src/parser/languages/index.ts</files>
  <action>
    Configure `web-tree-sitter` to load WASM grammars for Java, C++, Go, and Rust. Implement a mapping table in `src/parser/languages/index.ts` that maps language-specific node types to normalized types (e.g., `class_declaration` $\to$ `class`, `method_declaration` $\to$ `method`) per the patterns in RESEARCH.md.
  </action>
  <verify>
    <automated>
      # Unit test: pass a Java class string to the parser and verify it returns a normalized 'class' symbol.
    </automated>
  </verify>
  <done>WASM grammars loaded and normalization maps implemented for all 4 new languages.</done>
</task>

<task type="auto">
  <name>Task 2: Symbol Walker Expansion</name>
  <files>src/parser/symbol-walker.ts</files>
  <action>
    Update the symbol extraction walker to use the new normalization maps. Ensure it handles language-specific name fields (e.g., `declarator` in C++ vs `name` in TS) by using a per-language `name_fields` check as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>
      # Run `ctx build` on a fixture containing Java and Rust files; verify symbols are inserted into the `symbols` table.
    </automated>
  </verify>
  <done>Symbol walker correctly extracts and normalizes symbols from all 8 supported languages.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-lang Complexity Heuristics</name>
  <files>src/parser/symbol-walker.ts</files>
  <action>
    Implement complexity heuristics for the new languages based on keyword counts (e.g., `if`, `for`, `while`, `switch`). Ensure the logic matches the existing Py/TS/JS implementation to maintain consistent confidence scoring.
  </action>
  <verify>
    <automated>
      # Verify that a complex Rust function and a simple one have different complexity scores in the DB.
    </automated>
  </verify>
  <done>Complexity scoring active for all supported languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WASM Binary $\to$ Runtime | Loading external grammar binaries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering | WASM Grammars | medium | mitigate | Load grammars only from trusted internal paths or verified URLs. |
</threat_model>

<verification>
- `ctx build` successfully indexes a polyglot repo (TS, Java, Rust).
- Symbols from all languages appear in `ctx retrieve` results.
</verification>

<success_criteria>
- All 8 languages are indexed.
- Symbol normalization works across all languages.
</success_criteria>

<output>
Create `.planning/phases/04-merge-dataflow-multilang/04-02-SUMMARY.md` when done
</output>

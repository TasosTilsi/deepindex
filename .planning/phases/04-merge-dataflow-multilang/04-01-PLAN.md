---
phase: 04-merge-dataflow-multilang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/db.ts
  - src/parser/sql-extractor.ts
  - src/graph/projection.ts
  - src/cli.ts
autonomous: true
requirements:
  - DFLW-01
  - DGRPH-01
  - IMPT-01
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 20000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - A simple SQL CREATE TABLE statement is extracted and stored in SQLite.
    - A corresponding SELECT query is linked to that table.
    - `ctx impact <table_name>` returns the file containing the SELECT query.
  artifacts:
    - src/graph/db.ts (Schema v3 updates)
    - src/parser/sql-extractor.ts (Minimal regex path)
    - src/graph/projection.ts (Basic adjacency map)
  key_links:
    - SQL Extractor $\to$ SQLite Table $\to$ Graph Projector $\to$ Impact Query
---

<objective>
Implement a production-quality tracer slice that proves the end-to-end data-flow pipeline: from source code SQL extraction to impact analysis.

Purpose: Verify the architectural path from raw text to graph query before scaling to all languages and complex parsers.
Output: A working "SQL $\to$ Impact" vertical slice.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-merge-dataflow-multilang/04-CONTEXT.md
@.planning/phases/04-merge-dataflow-multilang/04-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "SQL Impact" — one path only</name>
  <files>
    src/graph/db.ts,
    src/parser/sql-extractor.ts,
    src/graph/projection.ts,
    src/cli.ts
  </files>
  <action>
    1. Update `src/graph/db.ts` to schema v3: Add `sql_queries` (id, query_text, file_id) and `query_tables` (query_id, table_name) tables.
    2. Create `src/parser/sql-extractor.ts`: Implement a minimal regex-based extractor for `CREATE TABLE` and `SELECT FROM` (porting basic patterns from RESEARCH.md).
    3. Create `src/graph/projection.ts`: Implement a basic adjacency map that loads `query_tables` into a `Map&lt;tableName, Set&lt;queryId&gt;&gt;`.
    4. Update `src/cli.ts`: Add `ctx impact &lt;table&gt;` command that uses the projection to find and print the files containing the queries.
    5. Wire the SQL extractor into the existing `ctx build` pipeline as a post-parse pass.
  </action>
  <verify>
    <automated>
      # Create a fixture file with "CREATE TABLE users..." and "SELECT * FROM users..."
      # Run: pnpm run ctx build ./fixture
      # Run: pnpm run ctx impact users
      # Verify output contains the fixture file path.
    </automated>
  </verify>
  <done>The single happy path (SQL extraction $\to$ SQLite $\to$ Impact Query) works end-to-end and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System $\to$ Indexer | Untrusted source code is parsed and stored in SQLite. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | SQLite Writes | medium | mitigate | Use parameterized queries for all SQL insertions to prevent injection from malformed source code. |
| T-04-SC | Tampering | npm installs | high | mitigate | package-legitimacy gate + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- Verify `ctx build` populates `sql_queries` and `query_tables` tables.
- Verify `ctx impact` returns correct file mapping.
</verification>

<success_criteria>
- `ctx impact` successfully resolves a table name to a source file.
- SQLite schema v3 is established.
</success_criteria>

<output>
Create `.planning/phases/04-merge-dataflow-multilang/04-01-SUMMARY.md` when done
</output>

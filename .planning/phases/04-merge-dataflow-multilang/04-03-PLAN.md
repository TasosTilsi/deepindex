---
phase: 04-merge-dataflow-multilang
plan: 03
type: tdd
wave: 3
depends_on: [04-01]
files_modified:
  - src/parser/sql-extractor.ts
autonomous: true
requirements:
  - DFLW-01
  - DFLW-02
  - DFLW-03
  - DFLW-04
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - Complex SQL queries with nested parentheses are correctly split into columns.
    - ORM annotations (@Table, @Entity) are extracted as table definitions.
    - MongoDB collection references are extracted.
    - XML/YAML config mappings are linked to tables.
  artifacts:
    - src/parser/sql-extractor.ts
  key_links:
    - Dual-path logic (Regex $\to$ formal parser)
---

<objective>
Implement the full SQL and data-flow extraction suite using TDD to handle edge cases like paren-depth and dialect variations.

Purpose: Provide high-fidelity extraction of the "data" layer of the codebase.
Output: A robust `SqlExtractor` class with dual-path (Regex + Formal) logic.
</objective>

<feature>
  <name>Dual-Path SQL &amp; ORM Extractor</name>
  <files>src/parser/sql-extractor.ts, tests/sql-extractor.test.ts</files>
  <behavior>
    - Case 1: `CREATE TABLE users (id INT, meta JSON(10,2))` $\to$ correct column split using paren-depth.
    - Case 2: `@Table(name="orders")` (Java/TS) $\to$ extract "orders" as table.
    - Case 3: `db.collection("logs")` (Mongo) $\to$ extract "logs" as table.
    - Case 4: Complex SELECT with JOINs $\to$ extract all referenced tables.
    - Case 5: Formal Path $\to$ fallback to `sql-parser-cst` for complex queries that regexes miss.
  </behavior>
  <implementation>
    1. Implement `splitColumnDefinitions` with paren-depth counter (ported from Cobi).
    2. Implement `RegexPath` using the patterns in RESEARCH.md.
    3. Implement `FormalPath` using `sql-parser-cst` for deep analysis.
    4. Create a coordinator that tries Regex first, then Formal, and logs the decision.
    5. Add extraction logic for ORM and Mongo patterns.
  </implementation>
</feature>

<verification>
  <automated>pnpm test tests/sql-extractor.test.ts</automated>
</verification>

<success_criteria>
- 100% pass rate on SQL edge-case test suite.
- Correct extraction of Table/Query relations for provided fixtures.
</success_criteria>

<output>
Create `.planning/phases/04-merge-dataflow-multilang/04-03-SUMMARY.md` when done
</output>

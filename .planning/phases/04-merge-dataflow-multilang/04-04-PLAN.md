---
phase: 04-merge-dataflow-multilang
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/graph/projection.ts
  - src/graph/queries.ts
  - src/cli.ts
autonomous: true
requirements:
  - DGRPH-01
  - DGRPH-02
  - DGRPH-03
  - IMPT-01
  - IMPT-02
  - PSTR-01
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - SQLite relations are projected into an in-memory adjacency map for fast BFS.
    - `ctx impact <table>` performs a multi-hop walk (Table $\to$ Query $\to$ File $\to$ Service).
    - `ctx parallel-storage` identifies tables appearing in different storage systems.
  artifacts:
    - src/graph/queries.ts
  key_links:
    - SQLite $\to$ Adjacency Map $\to$ BFS $\to$ Impact Report
---

<objective>
Project the stored data-flow indices into a queryable graph and implement high-level analysis tools.

Purpose: Transform raw table relations into actionable impact and storage insights.
Output: Impact and Parallel-Storage analysis tools.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-merge-dataflow-multilang/04-CONTEXT.md
@.planning/phases/04-merge-dataflow-multilang/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Graph Projection</name>
  <files>src/graph/projection.ts</files>
  <action>
    Implement the full projection from SQLite to the adjacency map. Include logic for:
    - Table $\to$ Query (READS_FROM / WRITES_TO)
    - Query $\to$ File (CONTAINS)
    - File $\to$ Service (via path-based detection: %Service%, %Controller%, %Repository%) per DGRPH-02.
  </action>
  <verify>
    <automated>
      # Unit test: project a known set of 3 tables and 2 queries; verify BFS from table reaches the correct service.
    </automated>
  </verify>
  <done>Graph projection correctly maps the full data-flow chain from table to service.</done>
</task>

<task type="auto">
  <name>Task 2: Impact &amp; Parallel-Storage Analysis</name>
  <files>src/graph/queries.ts</files>
  <action>
    Implement `getImpact(tableName, changeType)`: perform a BFS on the projected graph to find all affected queries, files, and services.
    Implement `findParallelStorage()`: group tables by domain and flag those found in >1 system (e.g., both SQL and Mongo).
  </action>
  <verify>
    <automated>
      # Run `ctx impact <table>` and `ctx parallel-storage` on the fixture; verify the reported chain matches the source.
    </automated>
  </verify>
  <done>Impact and parallel-storage analysis are implemented and return derived (non-hardcoded) results.</done>
</task>

<task type="auto">
  <name>Task 3: CLI Interface Integration</name>
  <files>src/cli.ts</files>
  <action>
    Wire the analysis tools into the CLI:
    - `ctx list-tables`
    - `ctx find-table-usage &lt;table&gt;`
    - `ctx impact &lt;table&gt;`
    - `ctx parallel-storage`
  </action>
  <verify>
    <automated>
      # Verify all 4 commands return the expected data from the fixture.
    </automated>
  </verify>
  <done>All data-flow analysis commands are available in the CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SQLite $\to$ Graph | Large indices could cause memory exhaustion during projection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Denial of Service | Graph Projection | low | mitigate | Implement a projection size limit or stream from SQLite for massive repos. |
</threat_model>

<verification>
- `ctx impact` identifies a full chain: Table $\to$ Query $\to$ File $\to$ Service.
- `ctx parallel-storage` correctly identifies dual-storage tables.
</verification>

<success_criteria>
- Data-flow graph is fully projected and queryable.
- Impact and parallel-storage reports are accurate.
</success_criteria>

<output>
Create `.planning/phases/04-merge-dataflow-multilang/04-04-SUMMARY.md` when done
</output>

---
phase: 04-merge-dataflow-multilang
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - src/parser/tagger.ts
  - src/requirements/sync.ts
  - src/requirements/extractor.ts
  - src/cli.ts
autonomous: true
requirements:
  - CTXT-01
  - CTXT-02
  - REQ-01
  - REQ-02
  - REQ-03
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Tables/Queries are auto-tagged with domains based on keywords.
    - Requirements are ingested from JSON and split into atomic statements.
    - `ctx req-coverage` identifies requirements without linked code and vice versa.
  artifacts:
    - src/parser/tagger.ts
    - src/requirements/sync.ts
  key_links:
    - JSON Requirement $\to$ Atomic Split $\to$ SQLite $\to$ Code Link
---

<objective>
Implement context tagging for data-flow entities and the requirements traceability system.

Purpose: Add business context (domains) and external validation (requirements) to the technical graph.
Output: Context tagging engine and Requirement coverage reports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-merge-dataflow-multilang/04-CONTEXT.md
@.planning/phases/04-merge-dataflow-multilang/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Context Tagging Engine</name>
  <files>src/parser/tagger.ts</files>
  <action>
    Implement a heuristic-based tagger that assigns domain/region/system tags to tables and queries using keyword dictionaries. 
    Ensure `ctx impact` and `ctx parallel-storage` are updated to accept `--domain`, `--region`, and `--system` filters.
  </action>
  <verify>
    <automated>
      # Unit test: tag a table as "Payment" based on "invoice" keyword; verify filter excludes non-Payment tables.
    </automated>
  </verify>
  <done>Entities are auto-tagged and queries are filterable by domain/region/system.</done>
</task>

<task type="auto">
  <name>Task 2: Requirements Indexing &amp; Sync</name>
  <files>src/requirements/sync.ts, src/requirements/extractor.ts</files>
  <action>
    Implement the requirement ingestion pipeline:
    1. Sync: Load JSON feeds (with optional Atlassian MCP bridge).
    2. Extraction: Use the atomic statement patterns (Modal/Bullet) from RESEARCH.md to split large requirements into atomic pieces.
    3. Store: Write to `requirements` and `atomic_requirements` tables.
  </action>
  <verify>
    <automated>
      # Run `ctx index-requirements <file.json>`; verify atomic requirements are created in SQLite.
    </automated>
  </verify>
  <done>Requirements are ingested, split into atomic statements, and persisted.</done>
</task>

<task type="auto">
  <name>Task 3: Traceability &amp; Coverage Reporting</name>
  <files>src/cli.ts</files>
  <action>
    Implement `ctx req-coverage`:
    - Identify "orphan" requirements (no linked code).
    - Identify "untracked" code (no linked requirement).
    - Generate a report listing both sets.
  </action>
  <verify>
    <automated>
      # Run `ctx req-coverage` on a fixture with one unlinked requirement; verify it appears in the report.
    </automated>
  </verify>
  <done>Traceability report correctly identifies coverage gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| JSON Feed $\to$ Indexer | External requirements JSON can be malformed or oversized. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Req Sync | low | mitigate | Use Zod to validate JSON schema before processing. |
</threat_model>

<verification>
- Requirements are correctly split into atomic statements.
- `ctx req-coverage` reports correct gaps.
- Domain filters correctly prune impact results.
</verification>

<success_criteria>
- Requirements traceability is fully operational.
- Domain-based filtering is implemented.
</success_criteria>

<output>
Create `.planning/phases/04-merge-dataflow-multilang/04-05-SUMMARY.md` when done
</output>
